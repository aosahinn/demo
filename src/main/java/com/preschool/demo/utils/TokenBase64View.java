/*
package com.preschool.demo.utils;

import com.aksigorta.domain.Token;

public class TokenBase64View extends Token {

    private Token token;

    public TokenBase64View(Token token) {
        this.token = token;
    }

    @Override
    public String getRefreshToken() {
        return TokenUtils.encodeBase64(token.getRefreshToken());
    }

    @Override
    public String getAccessToken() {
        return TokenUtils.encodeBase64(token.getAccessToken());
    }
}
*/
